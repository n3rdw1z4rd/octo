{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Debug",
            "windows": {
                "command": "${workspaceFolder}/build-debug.bat",
            },
            "linux": {
                "command": "${workspaceFolder}/build-debug.sh",
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}